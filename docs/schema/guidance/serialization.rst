Serialization
=============

BODS data should be serialized as a collection of JSON objects.

When providing data as a single file, objects should be contained within an ordered JSON array. 

JSON Lines may also be used when creating large files. 

